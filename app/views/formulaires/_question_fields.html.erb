<div class="nested">
   <div class ="nested-fields">  
       </br>
          <hr>
            <%= f.label :nom, "Question :" %> </br>
                <span>
                  <%= f.text_field :nom, placeholder: "Nom de la question", size: 75%>
                  <%= f.label :typequestion, "Type de question" %>
                  <%= f.select :typequestion, Question.typequestions.keys.map(&:humanize).zip(Question.typequestions
                  .keys), placeholder: "Type", class: "form-control" %>
                              <i class="fa fa-question-circle-o"  
                              title="Choix multiple : gngngngn - Choix simple : gngngn -   Réponse simple : gnngn">
                              </i> 
                    <%= link_to_remove_association '', f, class: 'btn btn-danger fa fa-trash-o'%>
            <div class="row">
              <div class="col-md-4">
                <span class="btn btn-default btn-file">
                  <i class="fa fa-picture-o fa-lg"></i> 
                  <%= file_field_tag "images[]", type: :file, multiple: :true %>
                </span>
              </div>
            </div>                    

                </span></br>
            
              <%= f.fields_for :answers do |builder2| %>
              <%= render 'answer_fields', f: builder2 %>
              <% end %>
            
        </hr>
              <span>
                    <%= link_to_add_association 'Ajouter une option', f, :answers, class: "btn btn-primary" %>
              </span> <br/> 
  </div> 
</div>
<br/> 
