<div class="fields">
      
  <%= f.label :nom, "Question :" %>
  <%= f.text_field :nom, placeholder: "Nom de la question", class: "form-control" %></br>
  
  <%= f.label :typequestion, "Type de la question" %>
  <%= f.select :typequestion, Question.typequestions.keys.map(&:humanize).zip(Question.typequestions
    .keys), placeholder: "Type", class: "form-control" %>
      <i class="fa fa-question-circle-o"  
                                     title="Choix multiple : gngngngn - 
                   Choix simple : gngngn -
                   Réponse simple : gnngn"></i><br /> 
                                    
 
  <%= link_to_remove_fields "remove", f %></br></br>
  
  <%= f.fields_for :answers do |builder| %>
    <%= render 'answer_fields', f: builder %>

  <% end %>

</div>