<%= form_for(@question) do |f| %>
  <!--
  <div class="field">
    <%= f.text_field :formulaire_id %>
  </div>
  -->
  <div class="row">
    <div class="form-group col-md-8">
        <label>Nom de la question :</label>
            <%= f.text_field :nom, placeholder: "Nom de la question", class: "form-control" %>
    </div>
  </div>

  <div class="row">
    <div class="form-group col-md-8">
        <label>Type de question :</label>
            <%= f.select :typequestion,[["Choix multiples",1],["Choix simple",2],["Reponse courte",3]],prompt: "Sélectionner le type de qu", class: "form-control" %>
    </div>
  </div>
  <!--
  <div class="field">
    <%= f.label :media %><br>
    <%= f.text_field :media %>
  </div>
  -->
  
    <!-- CHOIX  -->
      <%= f.fields_for :choixes do |fc| %>
        <%= fc.text_field :choix, placeholder: "-", class: "form-control" %>  
    
            
      <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
