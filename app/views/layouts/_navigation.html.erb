<nav class="navbar navbar-default navbar-static-top">
        <div class="navbar-header">
          
          <ul class="nav navbar-nav navbar-left">
                      <%= image_tag "agria.png",:size => "80x80" %>    
                      <%= link_to 'PI Agria', root_path, { :class=>"navbar-brand" } %>
          </ul>
        </div>
        <div class="navbar-collapse collapse navbar-right">
          <ul class="nav navbar-nav navbar-right">

                      <% if (!user_signed_in?) %>
                          <li><%= link_to 'Inscription', new_user_registration_path %></li>
                          <li><%= link_to 'Connexion', new_user_session_path %></li>
                      <% else %>
                          <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                <%= current_user.email %>
                                <span class="caret"></span></a>
                                <ul class="dropdown-menu">
                                        <li><%= link_to "blblbl", root_path %></li>
                                        <li role="separator" class="divider"></li>
                                        <li><%= link_to 'Déconnexion', destroy_user_session_path, method: :delete %></li>
                                </ul>
                        </li>
                    <% end %>
          </ul>
        </div>
</nav>